% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_rri_validation.R
\name{plot_rri_validation}
\alias{plot_rri_validation}
\title{Scatter of HRRI score against a simulator-defined target}
\usage{
plot_rri_validation(
  pool_agg,
  rri_col = "RRI",
  truth_col = "truth",
  colour_col = NULL,
  label_col = NULL,
  base_size = 9
)
}
\arguments{
\item{pool_agg}{Seed-level aggregate data frame with columns \code{RRI} and
\code{truth} (one row per seed).}

\item{rri_col}{Name of the HRRI column in pool_agg (default "RRI").}

\item{truth_col}{Name of the latent truth column (default "truth").}

\item{colour_col}{Optional column for point colour (e.g., "n_cycles").}

\item{label_col}{Optional column for point labels.}

\item{base_size}{Base font size.}
}
\value{
A ggplot object.
}
\description{
Plots mean RRI versus a declared synthetic target per aggregate row, annotated
with descriptive Pearson r and direct score-target RMSE, not LOO error.
No model is trained or held out by this plotting function.
}
\examples{
\dontrun{
  sim <- simulate_redox_holobiont(seed = 1)
  res <- rri_pipeline(soil = sim$Eh_stability, plant = sim$ROS_flux)
  agg <- data.frame(RRI = res$row_scores$RRI, truth = sim$latent_truth)
  plot_rri_validation(agg)
}
}
