% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hrri_infer_architecture.R
\name{hrri_infer_architecture}
\alias{hrri_infer_architecture}
\alias{print.hrri_arch}
\alias{summary.hrri_arch}
\alias{as.data.frame.hrri_arch}
\alias{validate_architecture}
\alias{print.hrri_arch_validation}
\title{Fit a conditional capacity-recovery curve (legacy function name)}
\usage{
hrri_infer_architecture(
  Eh_stability,
  id = NULL,
  perturb_time = NULL,
  tau_unit = c("day", "hour", "week"),
  group_cols = c("plot", "depth"),
  fit_edc = FALSE,
  min_points = 3L,
  verbose = TRUE,
  control = list(),
  baseline_end = NULL
)

\method{print}{hrri_arch}(x, ...)

\method{summary}{hrri_arch}(object, ...)

\method{as.data.frame}{hrri_arch}(x, row.names = NULL, optional = FALSE, ...)

validate_architecture(arch, params = NULL)

\method{print}{hrri_arch_validation}(x, ...)
}
\arguments{
\item{Eh_stability}{Data frame containing EAC and optionally EDC.}

\item{id}{Aligned identifiers containing time and grouping columns if absent.}

\item{perturb_time}{Disturbance peak time; NULL detects the EAC minimum.
Detection is descriptive and can select noise or the last observation.}

\item{tau_unit}{Time unit: day, hour or week.}

\item{group_cols}{Columns defining one trajectory. Include plant_id when
plant-level trajectories are separate; duplicate times are rejected.}

\item{fit_edc}{Also fit increasing EDC recovery toward its own baseline.
This is inappropriate for a decreasing EDC trajectory; default FALSE.}

\item{min_points}{Minimum distinct post-peak times (at least three).
This is a computational threshold, not proof of identifiability.}

\item{verbose}{Print fit status.}

\item{control}{Named list passed to stats::nls.control.}

\item{baseline_end}{Explicit end of the baseline window, strictly before
the peak. NULL uses only the first observation time as a stated assumption.}

\item{x}{An hrri_arch object.}

\item{...}{Additional method arguments.}

\item{object}{An hrri_arch object.}

\item{row.names, optional}{Standard data-frame method arguments.}

\item{arch}{An hrri_arch object with a keyed ground_truth data frame.}

\item{params}{Explicit strings of the form estimated_column:true_column.}
}
\value{
An hrri_arch object containing estimates, conditional fit status,
fit objects and optional summaries of supplied latent columns. Legacy columns
Q_eac, alpha_eac, k_eac and M_eac remain for compatibility. Prefer the aliases
baseline_eac, deficit_fraction_eac, recovery_rate_eac and terminal_ratio_eac.
M_eac is a terminal-to-baseline ratio, not a measure of causal memory.
k_eac_h converts the trajectory rate to reciprocal hours, not exchange kinetics.

Data frame with finite_pair flags and descriptive aggregate statistics.
The caller must establish that paired columns have the same definition and units.
}
\description{
Fits y(t) = B * (1 - A * exp(-r * t)) after a specified
disturbance peak. B is fixed from the observed baseline. A is a fractional
recovery deficit and r is a trajectory recovery rate. They are NOT the
accessibility alpha and reservoir exchange k in the accessible-capacity model.

Tabulates estimates and independently supplied targets.
There is no default mapping from recovery-curve amplitude to accessibility
or from recovery rate to reservoir exchange kinetics.
}
\details{
A nonlinear fit converging does not establish structural or practical
identifiability. Baseline uncertainty is not propagated into coefficient SEs.
With Cacc(t) = Q * alpha * (1 - exp(-k*t)), Q and alpha cannot be separated
using that curve alone. This function does not solve that inverse problem.
Fit only a single recovery window. Nonmonotonic and multi-event trajectories
require a different model and residual diagnostics.
}
\examples{
tt <- 0:12
yy <- ifelse(tt <= 2, 10, 10 * (1 - 0.7 * exp(-0.3 * (tt - 3))))
dat <- data.frame(plot = "P1", depth = "D1", time = tt, EAC = yy)
a <- hrri_infer_architecture(dat, perturb_time = 3, baseline_end = 2,
                             verbose = FALSE)
a$estimates
}
\seealso{
rri_recovery_metrics, rri_accessible_capacity
}
